/*  ex42a_strings  */
fn main() {
    let lista = vec![
	"Bill".to_string(), "Steve".to_string(),
	"Linus".to_string(), "Ken".to_string(),
    ];
    println!("{:?}", lista);
}
// ["Bill", "Steve", "Linus", "Ken"]

